# Copy this to .env and fill in values

# Environment
ENV=development

# CORS — comma-separated list of allowed frontend origins
# Development: leave as * 
# Production: ALLOWED_ORIGINS=https://your-frontend.vercel.app
ALLOWED_ORIGINS=*

# Port (uvicorn default is 8000)
PORT=8000
